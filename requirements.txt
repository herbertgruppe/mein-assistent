# LangChain Core
langchain
langchain-core
langchain-community

# LLM Providers
langchain-anthropic
langchain-openai

# Document Loaders
pypdf
docx2txt
pandas
openpyxl

# Web Search
tavily-python

# Utilities
python-dotenv
watchdog

# Web Interface
# >= 1.42: bringt st.login() / st.logout() / st.experimental_user für OIDC
streamlit>=1.58.0,<2.0

# PDF-Generierung aus Markdown-Protokollen
markdown
weasyprint

# Task Management
asana

# Microsoft Graph API
msal
requests

# Background Worker
apscheduler>=3.10.0
beautifulsoup4

# Auth: Rollen-Config (Login selbst läuft über Authentik / st.login)
pyyaml

# OIDC-Provider-Discovery für st.login (wird transitiv gezogen, aber explizit
# halten, damit Streamlit den OIDC-Stack korrekt aktiviert)
Authlib>=1.3.2

# REST-API (api.py) — Endpoint /api/process-reviewed-protocol für Cowork
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart
