# LangChain Core
langchain
langchain-core
langchain-community

# LLM Providers
langchain-anthropic
langchain-openai

# Document Loaders
pypdf
docx2txt
pandas
openpyxl

# Web Search
tavily-python

# Utilities
python-dotenv
watchdog

# Web Interface
# >= 1.42: bringt st.login() / st.logout() / st.experimental_user für OIDC
streamlit>=1.62.0,<2.0

# PDF-Generierung aus Markdown-Protokollen
markdown
weasyprint

# Task Management
asana

# Microsoft Graph API
msal
requests

# Background Worker
apscheduler>=3.11.3
beautifulsoup4

# Auth: Rollen-Config (Login selbst läuft über Authentik / st.login)
pyyaml

# OIDC-Provider-Discovery für st.login (wird transitiv gezogen, aber explizit
# halten, damit Streamlit den OIDC-Stack korrekt aktiviert)
Authlib>=1.7.2

# REST-API (api.py) — Endpoint /api/process-reviewed-protocol für Cowork
fastapi>=0.141.1
uvicorn[standard]>=0.52.4
python-multipart

# Protokoll-Review-Editor (templates/review.html)
jinja2

# Security: fix GHSA-rwj8-pgh3-r573 (env-var exfiltration via git remote URLs)
#           fix GHSA-4gmw-gg2m-w46p (unguarded git read-tree option forwarding)
gitpython>=3.1.58

# Security: fix CVE-2026-69244 (aiohttp DoS via malformed HTTP — transitive dep)
aiohttp>=3.14.3
